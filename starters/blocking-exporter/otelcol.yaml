receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

exporters:
  otlp:
    endpoint: example.com:4317
    sending_queue:
      enabled: false

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]

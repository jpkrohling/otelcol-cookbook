receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

exporters:
  debug:
    verbosity: detailed

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug]
  telemetry:
    resource:
      attributes:
        - name: service.name
          value: otelcol-own-telemetry
    metrics:
      level: detailed
      readers:
        - periodic:
            exporter:
              otlp:
                protocol: http/protobuf
                # This endpoint belongs to LGTM's separate Collector. Sending a
                # Collector's own telemetry back to itself is a feedback-loop anti-pattern.
                endpoint: http://localhost:4318/v1/metrics
                compression: gzip

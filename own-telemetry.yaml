receivers:
  otlp:
    protocols:
      grpc:

exporters:
  logging:

processors:

extensions:

service:
  extensions:
  pipelines:
    traces:
      receivers: [otlp]
      processors:
      exporters: [logging]
  telemetry:
    logs:
      level: debug
      initial_fields:
        service: my-instance
    metrics:
      level: detailed

receivers:
  kafka:
    initial_offset: earliest
    brokers: localhost:9092
    topic: otlp-spans

exporters:
  nop:

extensions:

service:
  pipelines:
    traces:
      receivers:  [ kafka  ]
      exporters:  [ nop  ]

  telemetry:
    metrics:
      readers:
        - pull:
            exporter:
              prometheus:
                host: '0.0.0.0'
                port: 8889

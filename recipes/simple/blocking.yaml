receivers:
  otlp:
    protocols:
      grpc:

exporters:
  logging:
  otlp:
    endpoint: example.com:4317

processors:

extensions:

service:
  extensions:
  pipelines:
    traces/1:
      receivers: [otlp]
      processors:
      exporters: [otlp]

    traces/2:
      receivers: [otlp]
      processors:
      exporters: [logging]
